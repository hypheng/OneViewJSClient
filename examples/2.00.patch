diff --git a/examples/addEnclosure.js b/examples/addEnclosure.js
index 1086f56..9584fb4 100644
--- a/examples/addEnclosure.js
+++ b/examples/addEnclosure.js
@@ -29,10 +29,43 @@ co(function*() {
       uri: '/rest/enclosure-groups',
       resolveWithFullResponse: true,
       body: {
-        type: 'EnclosureGroupV300',
+        type: 'EnclosureGroupV200',
         name: DefaultEnclosureGroupName,
-        interconnectBayMappings: [],
-        interconnectBayMappingCount: 0,
+        interconnectBayMappings: [
+          {
+            "interconnectBay": 1,
+            "logicalInterconnectGroupUri": null
+          },
+          {
+            "interconnectBay": 2,
+            "logicalInterconnectGroupUri": null
+          },
+          {
+            "interconnectBay": 3,
+            "logicalInterconnectGroupUri": null
+          },
+          {
+            "interconnectBay": 4,
+            "logicalInterconnectGroupUri": null
+          },
+          {
+            "interconnectBay": 5,
+            "logicalInterconnectGroupUri": null
+          },
+          {
+            "interconnectBay": 6,
+            "logicalInterconnectGroupUri": null
+          },
+          {
+            "interconnectBay": 7,
+            "logicalInterconnectGroupUri": null
+          },
+          {
+            "interconnectBay": 8,
+            "logicalInterconnectGroupUri": null
+          },
+        ],
+        interconnectBayMappingCount: 8,
         stackingMode: 'Enclosure',
         configurationScript: '',
         uri: null,
@@ -91,7 +124,7 @@ co(function*() {
     }));
 
     // concurrency
-    if ((i + 1) % 2 === 0) {
+    if ((i + 1) % 4 === 0) {
       yield Promise.all(promises);
       promises = [];
     }
diff --git a/examples/addProfile.js b/examples/addProfile.js
index 9a7dfe4..43dc754 100644
--- a/examples/addProfile.js
+++ b/examples/addProfile.js
@@ -29,12 +29,11 @@ co(function*() {
           resolveWithFullResponse: true,
           body: {
             name: newProfileName,
-            type: 'ServerProfileV6',
+            type: 'ServerProfileV5',
             serverHardwareUri: config.createEmptyProfile ? null : server.uri,
             serverHardwareTypeUri: server.serverHardwareTypeUri,
             enclosureGroupUri: server.serverGroupUri,
             serialNumberType: 'Virtual',
-            iscsiInitiatorNameType: 'AutoGenerated',
             macType: 'Virtual',
             wwnType: 'Virtual',
             description: '',
@@ -56,10 +55,7 @@ co(function*() {
               overriddenSettings: [],
             },
             hideUnusedFlexNics: true,
-            iscsiInitiatorName: '',
-            osDeploymentSettings: null,
             localStorage: {
-              sasLogicalJBODs: [],
               controllers: [],
             },
             sanStorage: null,
@@ -71,7 +67,7 @@ co(function*() {
         if (postRes) {
           console.log(`server profile is posted, task: ${postRes.headers.location}`);
           const profile = yield client.waitTaskComplete(postRes.headers.location).catch(err => {
-            console.log(`server profile ${newProfileName} is not created because ${err.taskErrors[0].message}`);
+            console.log(`server profile ${newProfileName} is not created because ${err.taskErrors ? err : err.taskErrors[0].message}`);
             return null;
           });
           if (profile) {
